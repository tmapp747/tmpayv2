import React from 'react';
import MobileProfile from './MobileProfile';

const ProfilePage: React.FC = () => {
  // Currently using mobile version for all devices
  return <MobileProfile />;
};

export default ProfilePage;